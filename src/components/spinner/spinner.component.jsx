import { SpinnerContainer, SpinnerOverlay } from "./spinner.styles";

// Spinner component
const Spinner = () => (
  <SpinnerOverlay>
    <SpinnerContainer />
  </SpinnerOverlay>
);

export default Spinner;
